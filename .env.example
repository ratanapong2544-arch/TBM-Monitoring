# Copy this file to .env and paste your ROTATED Gemini API key. Do NOT commit .env.
# Interim stopgap only — REACT_APP_* is baked into the client bundle and is NOT truly secret.
# Permanent fix: proxy the call via Google Apps Script — see gas/GEMINI_PROXY_SETUP.md
REACT_APP_GEMINI_KEY=
