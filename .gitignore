node_modules
build

# Secrets — local only (.env.example stays tracked)
.env
.env.local

# superpowers scratch (SDD ledger, brainstorm visual-companion temp)
.superpowers/

# local feature worktrees
.worktrees/
