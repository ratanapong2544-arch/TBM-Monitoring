# แผนที่แนวอุโมงค์ 3D — เพิ่มระยะทาง + ค่า chainage แต่ละปล่อง

วันที่: 2026-07-17 · สถานะ: approved (user เคาะครบ 4 ข้อ)

## ที่มา

User แคปหน้า Executive Dashboard (`AlignmentMapView`) ส่งรายงานทุกวัน ขอเพิ่มข้อมูลวิศวกรรม 2 อย่างลงบนแผนที่
พร้อม mockup: (1) ระยะทางระหว่างปล่อง หมุนตามแนวเส้น (2) ค่า chainage ของแต่ละปล่อง

## ข้อเท็จจริงที่ verify แล้ว (สำคัญ — อย่าลืม)

แนว KMZ ใน `alignmentGeo.js` กับตารางระยะทางทางการใน `constants.js` **ไม่ตรงกัน 44.195 ม.**
และส่วนต่างทั้งหมด **กระจุกอยู่ช่วง IS2→IS1 ช่วงเดียว** — อีก 2 ช่วงตรงกันถึงระดับมิลลิเมตร:

| ช่วง | ผลต่าง CH (KMZ) | ตารางทางการ | ตรงกัน? |
|---|---|---|---|
| IS4→IS3 | 3,065.962 | 3,065.962 | ✅ |
| IS3→IS2 | 4,093.624 | 4,093.624 | ✅ |
| IS2→IS1 | 1,670.902 | 1,715.097 | ❌ ต่าง 44.195 |
| **รวม** | **8,830.488** | **8,874.683** | |

ตัวเลข CH ใน mockup ของ user (IS2 = 1+685.811) ไม่ตรงกับทั้งสองระบบ → **ไม่ใช้** (zero hallucination)

## การตัดสินใจของ user (2026-07-17)

1. **"ค่าพิกัด" = chainage** (ไม่ใช่ lat/lng หรือ UTM)
2. **ป้าย CH ยึดระบบ KMZ** — เหตุผล: ตรงกับ CH หัวเจาะ (callout `CH 7+863`), ตรงกับตำแหน่งจริงบนแผนที่,
   และตรงกับ `profileGeo.js` ที่ใช้ชุดเดียวกันอยู่แล้ว → ไม่ต้องแก้ data เดิม
3. **ป้ายระยะทางยึดเลขทางการ** — เป็นเอกสารสัญญาที่เจ้าของงานคาดหวัง และเป็นตัวที่ใช้คิด % ความคืบหน้า
   ⚠️ **ผลที่ยอมรับแล้ว:** ช่วง IS2→IS1 ป้ายระยะ (1,715.097) จะลบกับผลต่าง CH บนภาพ (1,670.902) ไม่ได้
   user รับทราบและเลือกทางนี้เอง — **ห้ามมีใครมา "แก้ให้ตรงกัน" ทีหลังโดยไม่ถาม**
4. **CH วางเป็นบรรทัดในป้ายอาคารเดิม** ไม่ใช่กล่องแยกตาม mockup — เพราะระบบกันป้ายทับปรับจูนมาหลายรอบ
   (บทเรียนเก่า: เพิ่ม element เข้า collision เคยทำป้าย IS3 หายทั้งป้าย)

## ค่าที่จะใช้

| ปล่อง | CH ที่โชว์ | ที่มา |
|---|---|---|
| IS1 บางบัว | `0+000.000` | BP ของแนว KMZ |
| IS2 หลักสี่ | `1+670.902` | 8830.488 − 7159.586 |
| IS3 บางเขน | `5+764.526` | 8830.488 − 3065.962 |
| IS4 รัชดา | `8+830.488` | `CH_EXCAV_START` |

ระยะทาง: IS4→IS3 `3,065.962 ม.` · IS3→IS2 `4,093.624 ม.` · IS2→IS1 `1,715.097 ม.`

## สิ่งที่จะเปลี่ยน

### `src/utils/alignmentGeo.js` (pure data — jest-safe, ห้าม import three/maplibre)

- `SHAFTS[].ch` — ขยายความละเอียดจากทศนิยม 1 ตำแหน่งเป็นค่าเต็ม (8830.5→8830.488 ฯลฯ)
  ปลอดภัย: `ch` ปัจจุบัน**ไม่มีใครอ่าน** (`AlignmentMapView` ใช้แค่ name/fullName/role/capacity/lng/lat/labelDir)
- เพิ่ม `SHAFT_SPANS` — ระยะทางทางการ 3 ช่วง + คอมเมนต์บันทึกส่วนต่าง 44.195 ม. และเหตุผล

### `src/utils/alignmentGeo.test.js` (ใหม่)

ล็อกตัวเลขไม่ให้ถูกแก้โดยบังเอิญ:
- ผลรวม `SHAFT_SPANS` = `TOTAL_ROUTE_DISTANCE` (8,874.683)
- `SHAFTS.ch` ตรงกับ `CH_EXCAV_START − ระยะสะสม` และเรียงลดหลั่นถูกต้อง
- ส่วนต่าง KMZ↔ทางการ = 44.195 อยู่ที่ช่วง IS2→IS1 ช่วงเดียว (regression guard ของสมมติฐานทั้ง spec)

### `src/components/views/AlignmentMapView.jsx`

1. `fmtCH(ch, dp = 0)` — รับจำนวนทศนิยม default 0 → callout หัวเจาะ + popup instrument ไม่เปลี่ยน
2. ป้ายอาคาร: เพิ่ม `<span class="ch">CH x+xxx.xxx</span>` ท้าย `.lab` · ซ่อนเมื่อ `.mini` และบนมือถือ
3. ป้ายระยะทาง: marker ที่กึ่งกลางช่วง (`lngLatAtCh((chA+chB)/2)`) — ตัวหนังสือขาวมีเงา ไม่มีกล่อง
   - **มุมหมุน** = `atan2` ของ `map.project()` 2 จุดบนแนว → ได้มุมบนจอจริงรวมผล pitch+bearing ให้เอง
   - พลิก 180° เมื่อจะกลับหัว (คุมมุมไว้ใน −90..90)
   - หมุนที่ `<span class="lab">` ลูก **ห้ามหมุน marker root** (maplibre เขียน inline transform ทับ)
4. Collision: ป้ายระยะเข้า `shaftMarkersRef` ด้วย priority ต่ำสุด + ธง `noMini` (ไม่มีรูปแบบย่อ → ซ่อนเลย)
   - หมุน**ก่อน**วัด `getBoundingClientRect` เสมอ ไม่งั้นกรอบผิด
   - ใช้ class `.lab` กับตัวที่หมุน → โค้ด collision เดิมวัดถูกโดยไม่ต้องแก้ selector

## ที่ไม่ทำ (YAGNI)

- toggle เปิด/ปิด — user ไม่ได้ขอ (มี toggle เครื่องมือตรวจวัดอยู่แล้ว ถ้าอยากได้ค่อยเพิ่ม)
- ระยะจากหัวเจาะถึงปล่องถัดไป — mockup ไม่มี
- ลากป้ายระยะทาง — ป้ายอาคารลากได้อยู่แล้ว ป้ายระยะไม่จำเป็น

## เจอระหว่างทาง (นอกขอบเขต — แจ้ง user แล้ว)

บนจอ ≤640px ป้ายอาคาร**ว่างเปล่า**: CSS mobile ซ่อน `em`+`small` ส่วน `b` ถูกซ่อนตั้งแต่รอบแก้ 2026-07-16
→ ไม่เหลืออะไรแสดง นอกจากตอนโดนย่อเป็น `.mini` — **user เลือกยังไม่แก้รอบนี้** (คนละเรื่องกับที่สั่ง)
ถ้าจะแก้ทีหลัง: ให้ `b` กลับมา `display:block` ในบล็อก `@media (max-width:640px)`

## Verify

- `npx jest` — baseline 569 ต้องไม่ตก
- `CI=true npm run build` ต้องผ่าน (ESLint strict — บทเรียน labeled break)
- **Playwright + localhost** เท่านั้น — Browser pane ใช้ verify maplibre ไม่ได้
  (tab `visibilityState:hidden` → rAF ไม่ยิง → `map.on("load")` ไม่ fire → markers ไม่ขึ้นเลย)
- เช็คเชิงปริมาณ: อ่านข้อความป้ายจริง + `getBoundingClientRect` ทุกป้ายหาคู่ที่ทับ + ทดสอบหลาย zoom/viewport
